import { TrainingMemberDTO } from './training-member-dto';

describe('TrainingMemberDTO', () => {
  it('should create an instance', () => {
    expect(new TrainingMemberDTO()).toBeTruthy();
  });
});
